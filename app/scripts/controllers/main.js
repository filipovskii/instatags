'use strict';

angular.module('instatags')
  .controller('MainCtrl', function ($scope) {

    $scope.tagExamples = [
      'art',
      'puppies',
      'insta',
      'vsco',
      'kittens'
    ];

  });
